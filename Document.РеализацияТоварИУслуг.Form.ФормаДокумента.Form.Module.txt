
&НаСервере
Функция ТоварыНоменклатураПриИзмененииНаСервере(Номенклатура)
	КатегорияЦена =?(ПустаяСтрока(Объект.КатегорияЦен), Объект.Склад.УчетнаяКатегорияЦен, Объект.КатегорияЦен);
	Возврат МойМеханизм.ПолучитьЦен(Номенклатура,Объект.Дата,КатегорияЦена);	
КонецФункции

&НаКлиенте
Процедура ТоварыНоменклатураПриИзменении(Элемент)
	Стр = Элементы.Товары.ТекущиеДанные;	
	Стр.Цена = ТоварыНоменклатураПриИзмененииНаСервере(Стр.Номенклатура);
	Стр.Сумма = Стр.Цена * Стр.Количество;
КонецПроцедуры

&НаКлиенте
Процедура ТоварыКоличествоПриИзменении(Элемент)
	Стр = Элементы.Товары.ТекущиеДанные;
	Стр.Сумма = Стр.Цена * Стр.Количество;
КонецПроцедуры

&НаКлиенте
Процедура ТоварыСуммаПриИзменении(Элемент)
	Стр = Элементы.Товары.ТекущиеДанные;
	Попытка	
		Стр.Цена = Стр.Сумма / Стр.Количество;		
	Исключение	
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ТоварыПриИзменении(Элемент)
	СуммаДокумент = Объект.Товары.Итог("Сумма");
	СуммаКоличество = Объект.Товары.Итог("Количество");
КонецПроцедуры

&НаСервере
Процедура КатегорияЦенПриИзмененииНаСервере()
	Для каждого Стр Из Объект.Товары Цикл
	    КатегорияЦена =?(ПустаяСтрока(Объект.КатегорияЦен), Объект.Склад.УчетнаяКатегорияЦен, Объект.КатегорияЦен);
		Стр.Цена = МойМеханизм.ПолучитьЦен(Стр.Номенклатура,Объект.Дата,КатегорияЦена);		
		Стр.Сумма = Стр.Цена * Стр.Количество;
	КонецЦикла;	
КонецПроцедуры

&НаКлиенте
Процедура КатегорияЦенПриИзменении(Элемент)
	//Ответ = Вопрос("Савол",РежимДиалогаВопрос.ДаНет,,КодВозвратаДиалога.Да,"Жавоб");
	//Если Ответ = КодВозвратаДиалога.Да Тогда
		КатегорияЦенПриИзмененииНаСервере();		
	//КонецЕсли;
	//Если ПустаяСтрока(Объект.КатегорияЦен) Тогда
	//	СкладПриИзмененииНаСервере();
	//КонецЕсли;
	ТоварыПриИзменении(Элемент);
КонецПроцедуры

&НаСервере
Процедура СкладПриИзмененииНаСервере()
	Объект.КатегорияЦен = Объект.Склад.УчетнаяКатегорияЦен;
КонецПроцедуры

&НаКлиенте
Процедура СкладПриИзменении(Элемент)
	СкладПриИзмененииНаСервере();
КонецПроцедуры

&НаСервере
Процедура КлиентПриИзмененииНаСервере()
	Объект.ТорговыйАгент = Объект.Клиент.ТорговыйАгент;
КонецПроцедуры

&НаКлиенте
Процедура КлиентПриИзменении(Элемент)
	КлиентПриИзмененииНаСервере(); 
КонецПроцедуры

&НаКлиенте
Процедура Подбор(Команда)
	ОткрытьФорму("Документ.РеализацияТоварИУслуг.Форма.ПодборВсе",,ЭтаФорма);
КонецПроцедуры
