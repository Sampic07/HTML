
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Не Константы.ИспользоватьЦеновыеАкции.Получить() Тогда
		Элементы.ТекстАкции.Видимость = Ложь;
	КонецЕсли;
	
	Если Параметры.Свойство("ИсторияЦен") Тогда 
		Заголовок = Параметры.Заголовок; 
		РежимОткрытияОкна = Параметры.РежимОткрытияОкна;
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	ТекСтр = Элементы.Список.ТекущиеДанные;
	Если ТекСтр.Свойство("Регистратор") Тогда 
		ОткрытьЗначение(ТекСтр.Регистратор);
	КонецЕсли;	
КонецПроцедуры
