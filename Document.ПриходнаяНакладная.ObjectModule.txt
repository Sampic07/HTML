
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	//Для Каждого ТекСтрокаСписок Из Товары Цикл
	//	Если ПустаяСтрока(ТекСтрокаСписок.ПартияНоменклатура) Или ПустаяСтрока(ТекСтрокаСписок.ПартияНоменклатура.Код) Тогда
	//		НовыйПартия = Справочники.ПартииНоменклатуры.СоздатьЭлемент();				
	//		НовыйПартия.Владелец = ?(ТекСтрокаСписок.Номенклатура.ИспользоватьХарактеристикиНоменклатуры,ТекСтрокаСписок.ХарактеристикаНоменклатуры,ТекСтрокаСписок.Номенклатура);				
	//		//НовыйПартия.ГоденДо = ТекСтрокаСписок.СрокГодност;
	//		НовыйПартия.СебестоимостьШтука = ТекСтрокаСписок.Цена;
	//		НовыйПартия.ИспользоватьХарактеристикиНоменклатуры = ТекСтрокаСписок.Номенклатура.ИспользоватьХарактеристикиНоменклатуры;
	//		//НовыйПартия.СерииТовар = ТекСтрокаСписок.СерииТовар;
	//		//НовыйПартия.НДС = ТекСтрокаСписок.НДС;
	//		НовыйПартия.СсылкаПоступления = Ссылка;			
	//		НовыйПартия.Наименование = Строка(Формат(Ссылка.Дата,"ДФ=dd.MM.yyyy") + ": " + Строка(Поставщик) + ": " + ТекСтрокаСписок.Количество+"x"+Формат(ТекСтрокаСписок.Цена,"ЧГ=0"));
	//		НовыйПартия.Записать();
	//		ТекСтрокаСписок.ПартияНоменклатура = НовыйПартия.Ссылка;
	//	КонецЕсли;
	//КонецЦикла;		
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	//Для Каждого ТекСтрокаСписок Из Товары Цикл
	//		Партия = ТекСтрокаСписок.ПартияНоменклатура.ПолучитьОбъект();
	//		Партия.Владелец = ?(ТекСтрокаСписок.Номенклатура.ИспользоватьХарактеристикиНоменклатуры,ТекСтрокаСписок.ХарактеристикаНоменклатуры,ТекСтрокаСписок.Номенклатура);				
	//		//Партия.ГоденДо = ТекСтрокаСписок.СрокГодност;
	//		//Партия.НДС = ТекСтрокаСписок.НДС;
	//		Партия.СебестоимостьШтука = ТекСтрокаСписок.Цена;
	//		//Партия.СерииТовар = ТекСтрокаСписок.СерииТовар;
	//		Партия.ИспользоватьХарактеристикиНоменклатуры = ТекСтрокаСписок.Номенклатура.ИспользоватьХарактеристикиНоменклатуры;
	//		Партия.СсылкаПоступления = Ссылка;
	//		//НоменклатураДляНаименование = ?();
	//		Партия.Наименование = Строка(Формат(Ссылка.Дата,"ДФ=dd.MM.yyyy") + ": " + Строка(Поставщик) + ": " + ТекСтрокаСписок.Количество+"x"+Формат(ТекСтрокаСписок.Цена,"ЧГ=0"));
	//		Партия.Записать();	
	//КонецЦикла;	
	//Сообщить("ввввввввввв");
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, Режим)
	//{{__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!

	// регистр ОстаткиНоменклатуры Приход
	Движения.ОстаткиНоменклатуры.Записывать = Истина;
	Для Каждого ТекСтрокаТовары Из Товары Цикл
		Движение = Движения.ОстаткиНоменклатуры.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Приход;
		Движение.Период = Дата;
		Движение.Организация = Организация;
		Движение.Склад = Склад;
		Движение.Ячейка = ТекСтрокаТовары.Ячейка;
		Движение.Номенклатура = ТекСтрокаТовары.Номенклатура;
		Движение.ХарактеристикаНоменклатуры = ТекСтрокаТовары.ХарактеристикаНоменклатуры;
		Движение.Партия = ТекСтрокаТовары.ПартияНоменклатура;
		Движение.Количество = ТекСтрокаТовары.Количество;
	КонецЦикла;

	// регистр ЦеныНоменклатуры
	Движения.ЦеныНоменклатуры.Записывать = Истина;
	Для Каждого ТекСтрокаТовары Из Товары Цикл
		Движение = Движения.ЦеныНоменклатуры.Добавить();
		Движение.Период = Дата;
		Движение.Номенклатура = ТекСтрокаТовары.Номенклатура;
		Движение.ХарактеристикаНоменклатуры = ТекСтрокаТовары.ХарактеристикаНоменклатуры;
		Движение.КатегорияЦен = Склад.Владелец.КатегорияЦенЗакупочная;
		Движение.Цена = ТекСтрокаТовары.Цена;
	КонецЦикла;

	//}}__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
КонецПроцедуры

Процедура ПередУдалением(Отказ)
	Для каждого ТекСтрока Из Товары Цикл
		ТекСтрока.ПартияНоменклатура.ПолучитьОбъект().Удалить();
	КонецЦикла;
КонецПроцедуры
